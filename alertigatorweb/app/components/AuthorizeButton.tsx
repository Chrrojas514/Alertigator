import React from 'react'

function AuthorizeButton() {
  return (
    <button id='authorize_button' data-theme='alertigatorTheme' className='btn btn-primary'>
        Authorize Button
    </button>
  )
}

export default AuthorizeButton